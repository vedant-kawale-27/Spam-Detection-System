{
  "name": "spam-detection-browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome/Firefox extension for inline spam scanning in Gmail/Outlook (issue #187)",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
